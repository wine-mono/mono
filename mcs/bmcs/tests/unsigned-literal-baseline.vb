Class T
    Shared Sub Main()
        Dim a As ULong = 0UL
        Dim b As UInteger = 0UI
        Dim c As UShort = 0US
        Dim d As Double = 1#
        Dim e As Single = 1!
        Dim f As Decimal = 1@
        System.Console.WriteLine(a + CLng(b) + CLng(c))
    End Sub
End Class
